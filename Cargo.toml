[package]
name = "pantograph-rs"
version = "0.11.0"
edition = "2024"
description = "Convert between open vector geospatial formats (GeoJSON → GeoParquet), dependency-free."
license = "MIT"

[[bin]]
name = "panto"
path = "src/main.rs"

[dependencies]
